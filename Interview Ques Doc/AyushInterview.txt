Dentsu first round of Interview

- Introduction
- challenges via software developer
- most challenging problem ever faced
- Closures
- Event loop in detail
- Difference between Promise.all and Promise.race
- event delagation - i told down to up
- how to stop that propogation where event move from down to up

- local state, context api and redux - which one to use when
- useCallback and useMemo
- handle component level avoid re-rendering - memo
- how to handle large image asset as it make website ugly 
    - webP
    - CDN/cloudflare
    - preload
    - lazy load that component if it is below fold
    - progressive image loader ( I answered placeholder image)

- Write a program to achieve this code
    // input ['a', 'b', 'a', 'c', 'b', 'a']
 
    // output
    // {
    //   3: ['a'],
    //   2: ['b'],
    //   1: ['c']
    // }

-------------------------------------------------------------------------

Appinventiv first round of Interview

- Three types of rendering - Client side, Server side and Static - which one we use when
- When to use Context API and Redux? and What is in previous Redux and new Redux toolkit? Redux thunk and saga
- useImperativeHandle and useuselayouteffect and uselayout
- What introduced in React19?
- What aspect you consider in terms of security of website or how do you handle security on your website
- Which frontend security practices have you implemented?
- How to handle performance of website
- find the longest substring without duplicate characters

-------------------------------------------------------------------------

Intellias FE round

- Solid Principals mandatory or optional?
- What is Solid Principals?
- What is SSP in it?

-------------------------------------------------------------------------

Yatra First round of Interview

- useMemo, useCallback, React.memo
- React.memo -> if we have a button and on click any event happens - does that component re-render
- lifecycle methods
- compenentwillunmout in functional component
- when we return from useEffect, does that time gets cleaned or that component gets unmount?
- what is polyfill
- polyfill for groupby
- types of modules in js
- what is module federation?
- hoisting
- event loop
- If there are 4 timeout, and 1 timeout there is a promise, then order of execution
- youtube video streaming when we leave and when we reopen
- when we change quality of video, how it will do?
- what is LCP, FCP, CLS and TBT
- how did you measure reduce build time? what strategies and how did you identify this problem
- what is the value of LCP, CLS?
- what helps you in reducing build time by upgrading nextjs version from 11 to 15
- what is SSR, CSR, SSG and how did you implement it
- how it works in browser?
- what is garbage collector
- what is memory leak

-------------------------------------------------------------------------

- introudction
- coding on react
- api
- system design
- dsa
- leading

-------------------------------------------------------------------------

encora first round of internview - 24th feb

"A"+"B"
"A"+"B"-"A"
"a"-"b"
"a"*"b"

// console.log(0.1 + 0.2 == 0.3);
// console.log(0.1 + 0.2 === 0.3);

// let a =0;
// console.log(++a)

// const [count, setCount] = useState(0);

// useEffect(() => {
//   console.log(count++);
// }, [count]);

// const [count, setCount] = useState(0);
// useEffect(() => {
//   console.log(++count);
// }, [count]);
 
 
// function Counter() {
//   const [n, setN] = React.useState(0);
//   return (
// <button onClick={() => { setN(n + 1); setN(n + 1); }}>
//       {n}
// </button>
// );
// }

// [8,1,24,8,99,38,1,2]
// Asc
// Des we can use inbuilt function also

-------------------------------------------------------------------------

Dentsu second round of Interview - 24th feb

complex task that you worked on
prototype based inheritance in javascript
types of inheritance
find no. of pair in unsorted array that sum upto target value
works or setup ci/cd pipeline
daily schedule of work
loop of 1 to 10 with var and how to resolve it? why it happens and how can we resolve?

-------------------------------------------------------------------------

Yatra second round of Interview - 24th feb

- If we have large list like if we search for new delhi to mumbai there are 1000 results are coming from api, we ant to do it in 
  batches, is it possible in redux?
  we show 5 resuls, can we without re-render? do it with redux without re-render, subscribe?
- Web socket? how it works? what is webscoket? example
- multiple redux store can duable?
- how many fibre tree needed, if we delete one row from the tree of 1000 rows.
- how offline and online app works? git?
- how many fibre tree are there?
- time slicing
- react works on virtual dom, what user see's virtual dom or real dom?
- shoudyield
- react scheduler
- react diffing algorith works?
- how virtualization works? how intersection observor works?
- state management
- react fibre and its significance

-------------------------------------------------------------------------

lodhagroup / Bellavie firsr round of Interview - 24th feb

- count assignment react - state lift from child, key concept - check test assignment on system

-------------------------------------------------------------------------

altimetrik first round of Interview - 25th feb

- what is hoisting and how hoisting works?
- execution context?
- example of closure in real application that you faced
- what is stale closure?
- what is memory leakage? how it happens? how can we resolve
- what are es6 features?
- classes in javascript
- what is arrow function? and why it is ingtroduced? how it is difference from normal?
- How react works? how it starts
- what is singelton patterns
- what introduced in react 18 nad 19
- what is concurrent rendering
- setter function?
- useState is synchronous or asynchronous? how it works?
- important rules of hooks
- what is lifting the state up? ways to do it
- component composition
- useRef vs useState
- why useRef not changes when re-redner happens? 
- what is rendering and re-rendering means
- useEffect vs useLayoutEffect
- what is generics
- interfaces vs types
- utility types
- why do we use TypeScript? benefits
- 

-------------------------------------------------------------------------

kubigo first round of Interview - 25th feb

- General discussion
- if there is one application whuch is using 4 backend url, how can you deploy it as you need to change backend url
in your api calls in useEffect. How to achieve this
- there are two ways to deploy - either we can copy paste the files into /dist or we can run commoans like npm run build and then start on server
- why next, everybody is moving to turbopack instead of webpack?
- how you and backend team handle things?

-------------------------------------------------------------------------

olx first round of Interview - 25th feb

- General discussion

-------------------------------------------------------------------------

Valliance first round of Interview - 25th feb

- Outpput 
    useEffect(() => {
    console.log("Effect");
    });
    useEffect(() => {
    console.log("Effect");
    }, []);
    useEffect(() => {
    console.log("Effect");
    }, [count]);

- Problem in this code
    useEffect(() => {
    setCount(count + 1);
    }, [count]);

- how do we handle asynchronous code in redux
- we can pass function in dispatch via redux thunk and using useDispatch also? how it works?
- middleware in redux
- useSelector, useDispatch in redux
- redux vs context api ( when to use which )
- how to solve prop drilling? any other way except context api
- temportal dead zone? how it works internally? execution context?
- what is callback?
- callback hell? what is issue if there are nested callback? how it do asynchronous task
- what is asynchronous meant?
- passed function as props, how to avoid re-render in child if parent component re-render
- build a select dropdown of country, and if we select any country, it will show another dropdown with cities
    https://codesandbox.io/p/sandbox/nqcn8r?file=%2Fsrc%2FApp.js
- is callback sync or asynchronous

-------------------------------------------------------------------------

altimetrik Second round - 26th feb

- child div should places like top left, top roght, bottom left and bottom right
  how to place them using css?
    <div>
    <div></div>
    <div></div>
    <div></div>
    <div></div>
    </div>

- manifest in HTML
- web workers in html
- Semantic tags in html
- aria labels 

- how would you pick div inside a div.
    :has()
    nth-child

- what call bind and apply ( immediate call the function = call func?)
- bind and call difference
- apply method definition create or polyfill
    use bind to create apply

- Outpput
    for (var i = 0; i < 3; i++) {
    setTimeout(() => console.log(i), 1000);
    }
    
    sayHello(); 
    var sayHello = function() {
        console.log("Hello!");
    };
    
    Hello!
 
    console.log("A");
    setTimeout(() => console.log("B"), 0);
    Promise.resolve().then(() => console.log("C"));
    console.log("D");

 
    console.log(null == undefined);
  
- How many closures are there
    const [items, setItems] = useState([]);
    const refresh = async () => {
        const res = await fetch('callToAPIEndpoint');
        const data = await res.json();
        setItems([...items, ...res]);
    }
    useEffect(() => {
    // call refresh method here 
    refresh();
    }, [refresh])
 

// function countChar(str, targetStr){
//     // const hashMap = {};
//     // for(let i=0;i<str.length;i++){
//     //     if(!hashMap[str[i]]){
//     //         hashMap[str[i]] = 1
//     //     } else {
//     //         hashMap[str[i]] = hashMap[str[i]] + 1;
//     //     }
//     // }
    
//     let count = 0;
//     for(let i=0;i<str.length;i++){
//         if(str[i] == targetStr){
//             count += 1;
//         }
//     }
    
//     return count;
    
// }

// const result = countChar('GeeksForGeeks', 'G');
// console.log(result);

- Problem in this code
    // const [items, setItems] = useState([]);

    // const refresh = useCallback(async () => {
    //     const res = await fetch('callToAPIEndpoint');
    //     const data = await res.json();
    //     setItems((prev) => [...prev, ...data]); // setItems([...items,...data])
    // },[setItems]) // [items, setItems] 


    // useEffect(() => {
    //   // call refresh method here 
    //   refresh();
    // }, [refresh])

- Prototype chaining 
- when we build the react application? what will happen? (when we run npm run build - what will happen)

-----------------------------------------------------------------------------

xebia - (27th feb)

- Given an array of words arr[], the task is to groups strings that are anagrams. An anagram is a word or phrase formed by rearranging the letters of another, using all the original letters exactly once.
 
    Input: arr[] = ["act", "god", "cat", "dog", "tac"]
    Output: [["act", "cat", "tac"], ["god", "dog"]]
    Explanation: There are 2 groups of anagrams "god", "dog" make group 1. "act", "cat", "tac" make group 2.
    Input: arr[] = ["listen", "silent", "enlist", "abc", "cab", "bac", "rat", "tar", "art"]
    Output: [["abc", "cab", "bac"], ["listen", "silent", "enlist"],["rat", "tar", "art"]]
    Explanation: 
    Group 1: "abc", "bac" and "cab" are anagrams.
    Group 2: "listen", "silent" and "enlist" are anagrams.
    Group 3: "rat", "tar" and "art" are anagrams.

- Build a timer starts from 0, there are three button to start, stop and Reset! Post that it automatically stops at 10 sec and 
if we can click on start it resumes from 10 now.

    import { useEffect, useState } from "react";
    import "./styles.css";

    export default function App() {
    const [seconds, setSeconds] = useState(0);
    const [timerStart, setTimerStart] = useState(false);

    useEffect(() => {
        let timer;
        if (timerStart) {
        timer = setInterval(() => {
            setSeconds((prev) => {
            // if (prev == 10) {
            //   setTimerStart(false);
            //   return prev;
            // }
            return prev + 1;
            });
        }, 1000);
        }

        return () => {
        clearInterval(timer);
        };
    }, [timerStart]);

    useEffect(() => {
        if (seconds == 10) {
        setTimerStart(false);
        }
    }, [seconds]);

    const handleStart = () => {
        setTimerStart(true);
    };

    const handleStop = () => {
        setTimerStart(false);
    };

    const handleReset = () => {
        setTimerStart(false);
        setSeconds(0);
    };

    return (
        <div className="App">
        {/* <h1>Hello CodeSandbox</h1> */}
        <h2>Timer</h2>
        <p>Seconds: {seconds}</p>
        <div>
            <button onClick={handleStart}>Start</button>
            <button onClick={handleStop}>Stop</button>
            <button onClick={handleReset}>Reset</button>
        </div>
        </div>
    );
    }

-----------------------------------------------------------------------------

console.log('1'); 
fetch('/api/data') .then(res => res.json()) .then(data => console.log('2', data)); 
console.log('3');

console.log('1'); 
const data = await fetch('/api/data');
const result = await data.json();
console.log('2',result);
console.log('3');

output of both
- 1,3,2
- 1,2,3

-----------------------------------------------------------------------------

Tata digital 1st round (2nd march)

- where is caching applicable? redis on client side not possible? where is redis?
- two api call = one to cms and then magento - going to origin server ?
- Cache busting
- how do you identify memory leak?
- abort is default in fetch, what other methods are default that are used in helping us?
- network interxeptor? 
- setting token in localStograge is right? how to solve this?
- how do you render large list in react? without pagination
- virtualization impacts seo? how do you solve?
- can we use web socket or server? 
- what is closureS?
- what is closures in react?
- useState vs useReducer
- useDispatch
- custom hook for api fetch call

-----------------------------------------------------------------------------

Max IQ  1st round (2nd march)

- flatten array using depth
- Output

    function Child() {
    React.useEffect(() => {
        console.log("Child effect");
    });
    return <div>Child</div>;
    }

    function Parent() {
    React.useEffect(() => {
        console.log("Parent effect");
    });
    return <Child />;
    }

    const App = () => {
    return <Parent/>
    } 

- Two context and which re-render issue - https://medium.com/@thebishalniroula/the-problem-with-context-api-and-how-to-solve-it-with-children-fc41eab9925f

        import { createContext, useContext, useState } from "react";

        const ThemeContext = createContext();
        const UserContext = createContext();

        export const Child1 = () => {
        const user = useContext(ThemeContext);

        return <div>Ayush</div>;
        };

        export const Child2 = () => {
        const user = useContext(UserContext);

        return <div>Tibra</div>;
        };

        export default function App() {
        const [theme, setTheme] = useState("white");
        const [user, setUser] = useState("ayush");

        return (
            <ThemeContext.Provider value={{ theme, setTheme }}>
            <Child1 />
            <UserContext.Provider value={{ user, setUser }}>
                <Child2 />
            </UserContext.Provider>
            </ThemeContext.Provider>
        );
        }

- issue with this code

    export default function App() {
        const [theme, setTheme] = useState("white");
        const [count, setCount] = useState(0);

        useEffect(() => {
            setInterval(() => {
            console.log(count);
            }, 1000);
        }, []);

        return (
            <div>Ayush</div>
        );
    }


button onclick - memo and useCallback

- button set state - two update
    export default function App() {
        const [theme, setTheme] = useState("white");
        const [count, setCount] = useState(0);

        function handleClick() {
            setCount(count + 1);
        }

        return (
            <div>
            {count}
            <button onClick={() => setCount(count + 1)}>Hi</button>
            <button onClick={handleClick}>bye</button>
            </div>
        );
    }

------------------------------------------------------------------------------

Quarks First round (3rd March)
    - https://codesandbox.io/p/sandbox/hidden-snowflake-fgdnmp
    - debounce and fetch fun

------------------------------------------------------------------------------

Tata Digital second round (5th march)

    - Global toast based notification which support all type like success, warning, error messages
    - Can have multple notification and they should be stacked upon.
    - can trigger from anywhere in the application like any child, interceptor, etc.
    - how you are going to trigger it? like using redux middleware, etc.

------------------------------------------------------------------------------

Perforce 1st round interview (6th march)
    - how did you scale and high perform achieve?
        Not mention state manage library or stuff because it is incorporated, how did you scale?
    - filter polyfill ( why use .call?, what would happen if we not use this?, what are errors here)?
    - ai tolls that you used as a frontend

------------------------------------------------------------------------------

GSPANN 1st round interview(6th march)
    - which one to choose when React or NextJS
    - benefit of server components?
    - is getServerSideProp is same as Server component? why do we need server comp? what is Difference
    - Core Web Vital explain? Explain main 4 attribute root cause and fixes
    - Counter in react with max and min input field that adjust according to Counter
    - What to use flexbox and grid when? difference and use case
    - types of position in CSS can you can mention properties of it.
    - Tailwind css vs bootstrap
    - Island architecture
    - Next server component

------------------------------------------------------------------------------

Dimaag.AI 1st round interview(6th march)
    - Cause of memory leak, how it happens?
    - maximum call stack reached? why this error came? and how to resolve it
    - Let's start with the one scenario is, since you are a lead front-end here, okay, you have given a Figma design. Today I have been asked you to develop the front-end architecture, okay? Knowing current open-source technologies or packages or whatever we have in a front-end, whatever the NPM packages which are suitable for the front-end or React or Next.js, any framework in the front-end. So I want to have a full, I want to know the full talk about it, like start from an HTML tag and closing till an HTML tag. That means a full architecture, how will you plan to set up the architecture? Like considering all these things, like styling, how will you have your services for the integration? How will you be declaring your methods, different methods for post, get, put, all these methods? How will be your component structure and what are the packages we'll use? Okay, how will you maintain a config file, like a feature flag kind of thing? So I'm not sure I'm giving you more answers, right?
    - Let's start with the one scenario is, since you are a lead front-end here, okay, you have given a Figma design. Today I have been asked you to develop the front-end architecture, okay? Knowing current open-source technologies or packages or whatever we have in a front-end, whatever the NPM packages which are suitable for the front-end or React or Next.js, any framework in the front-end. So I want to have a full, I want to know the full talk about it, like start from an HTML tag and closing till an HTML tag. That means a full architecture, how will you plan to set up the architecture? Like considering all these things, like styling, how will you have your services for the integration? How will you be declaring your methods, different methods for post, get, put, all these methods? How will be your component structure and what are the packages we'll use? Okay, how will you maintain a config file, like a feature flag kind of thing? So I'm not sure I'm giving you more answers, right?
    - Event driven architecture?
    - Memory heap?
    - How garbage collector clears variable
    - As any field setup react with NPX or with Vite or anything like that, there are some in open config.ts or js, you'll see some Babel transformers up there. So why do we need these Babel transformers?

------------------------------------------------------------------------------

MAX IQ 2nd round interview (ARWA)(6th march)
    - VS Code Structure code with onclick hide and show folder
    - const [open, setOpen] = useState(false);
        const handleClick = () => { setOpen(!open) }

        What is the difference of setting state in useState, one with callback and another one is directly.
    - High level of flow and idea of redux
    - Flux pattern/architecture
    - what is virtualization and what problem it solved?
    - any other way we can implement the same thing in javascript
    - What is ShadowDom
    - When react preserve the state and when it reset the state on re-render
    - what is react query and why it is used? what problem it solved
    - and what about SWR
    - Downside of using React
    - What is memory leak?
    - Do you think memory leaks are different from a non-React application? Let's say we have a plain JavaScript application and we have a same application built in React. So, is there any difference between memory leaks of these two applications if we implement the same scenario, or there can be different pitfalls based on the framework like React?
    - what is garbage collector

------------------------------------------------------------------------------

var Temp1 =[{FirstName:"Vivek", Id:1},{FirstName:"Vinay", Id:2}]
 var Temp2 =[{LastName:"A", Id:2},{LastName:"D", Id:1}];
 //Expected Output = [{FirstName:’Vivek’, LastName:’D’, Id:1},{FirstName:’Vinay’, LastName:’A’, Id:2}]; 


console.log('1');
setTimeout(() =>console.log('2'), 0);
Promise.resolve().then(() =>console.log('3'));
requestAnimationFrame(() =>console.log('4'));
console.log('5');

Q: You have a dashboard that shows real-time stock prices and a static "About Us" page. Which rendering strategy (SSR, SSG, ISR, or CSR) would you use for each, and why?
 
Q: In the App Router, what is the difference between a Layout and a Template?
 
Q: How would you implement a "Like" button that feels instant, even if the server is slow?
 
Q: How do you implement Role-Based Access Control (RBAC) in a Next.js app?
 
Write a function once(fn) that ensures a given function can only be called exactly once. Subsequent calls should return the value from the first invocation.
 
once(fn);
once(fn);
 
How do you create an object that inherits from another without using the class keyword?
 
You have a dashboard page that needs to fetch UserPortfolio, MarketNews, and UserPreferences. Currently, you are awaiting them one by one in a Server Component. What is the performance issue here, and how do you fix it?
 
When using Server Actions, how do you handle form validation and display errors to the user?
 
Accessibility
vanilla js
bowser Repaint
core web vitals



























1